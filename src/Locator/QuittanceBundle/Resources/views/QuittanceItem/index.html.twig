<h1>QuittanceItem list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Header</th>
            <th>Price</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('quittanceitem_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.header }}</td>
            <td>{{ entity.price }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('quittanceitem_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('quittanceitem_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('quittanceitem_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
