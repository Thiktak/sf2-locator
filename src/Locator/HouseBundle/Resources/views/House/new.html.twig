{% extends "::layout.html.twig" %}

{% block menu %}
  {% render "LocatorHouseBundle:House:menu" %}
{% endblock %}

{% block body %}
        <header>
          <hgroup>
            <h1>{{ 'House creation'|trans }}</h1>
          </hgroup>
          <aside>
            <ul>
                <li>
                    <a href="{{ path('house') }}">
                        <i class="icon-list"></i>
                    </a>
                </li>
            </ul>
          </aside>
        </header>

        <form class="body form-horizontal" action="{{ path('house_create') }}" method="post" {{ form_enctype(form) }}>
          <div class="main main-form">
            {{ form_widget(form) }}
          </div>
          <div class="form-actions">
            <button type="submit" class="btn btn-primary">Create</button>
          </div>
        </form>
{% endblock %}